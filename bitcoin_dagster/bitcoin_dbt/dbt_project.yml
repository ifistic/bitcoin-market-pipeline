name: 'bitcoin_dbt'
version: '1.0.0'
config-version: 2

profile: 'bitcoin_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  bitcoin_dbt:
    bronze:
      +materialized: table
      +schema: bronze
    silver:
      +materialized: view
      +schema: silver
    gold:
      +materialized: view
      +schema: gold
