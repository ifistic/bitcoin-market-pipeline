locations:
  - location_name: bitcoin_market
    code_source:
      package_name: crypto_pipeline

    container_context:
      docker:
        container_kwargs:
          image: python:3.10
        env_vars:
          - "PATH=/usr/bin:$PATH"

   
