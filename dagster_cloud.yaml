locations:
  - location_name: bitcoin_market
    code_source:
      package_name: crypto_pipeline

    container_context:
      docker:
        container_kwargs: {}   # empty → standard Python base image
        env_vars:
          - "GIT_PYTHON_REFRESH=quiet"




   
